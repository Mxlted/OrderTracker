<Window x:Class="OrderTracker.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:OrderTracker.Desktop.Converters"
        xmlns:local="clr-namespace:OrderTracker.Desktop"
        xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
        xmlns:util="clr-namespace:OrderTracker.Desktop.Utilities"
        mc:Ignorable="d"
        Title="Order Tracker"
        MinHeight="720"
        MinWidth="1120"
        Height="860"
        Width="1360"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/app-icon.ico"
        Background="#0E1117">
    <Window.Resources>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0E1117"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#131923"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#171D28"/>
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#1D2633"/>
        <SolidColorBrush x:Key="ControlBrush" Color="#111722"/>
        <SolidColorBrush x:Key="ControlHoverBrush" Color="#223044"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2B3647"/>
        <SolidColorBrush x:Key="TextBrush" Color="#EEF3F8"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#97A4B5"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#5CC8FF"/>
        <SolidColorBrush x:Key="AccentDarkBrush" Color="#1E5870"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#2F9E7E"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E05D5D"/>
        <SolidColorBrush x:Key="DangerSurfaceBrush" Color="#53242A"/>
        <SolidColorBrush x:Key="DataGridHeaderBrush" Color="#101722"/>
        <SolidColorBrush x:Key="DataGridAlternateBrush" Color="#151B25"/>
        <SolidColorBrush x:Key="RowHoverBrush" Color="#1B2635"/>
        <SolidColorBrush x:Key="RowSelectedBrush" Color="#233A50"/>
        <SolidColorBrush x:Key="ProgressTrackBrush" Color="#101722"/>
        <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#101722"/>
        <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#405067"/>
        <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#5D708C"/>
        <SolidColorBrush x:Key="LinkHoverBrush" Color="#A7E4FF"/>
        <SolidColorBrush x:Key="ChipTextBrush" Color="#0E1117"/>
        <SolidColorBrush x:Key="TrackingChipBrush" Color="#102332"/>
        <SolidColorBrush x:Key="TrackingChipBorderBrush" Color="#244A62"/>
        <SolidColorBrush x:Key="ModalOverlayBrush" Color="#99000000"/>

        <conv:PageVisibilityConverter x:Key="PageVisibility"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <conv:BoolToGridLengthConverter x:Key="BoolToGridLength"/>
        <conv:StatusBrushConverter x:Key="StatusBrush"/>
        <conv:MerchantBrushConverter x:Key="MerchantBrush"/>
        <util:BindingProxy x:Key="VmProxy" Data="{Binding}"/>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="PageTitleStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
        </Style>

        <Style x:Key="SettingsCardStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentDarkBrush}"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
        </Style>

        <Style x:Key="PlaceholderTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="InputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                MinHeight="{TemplateBinding MinHeight}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"/>
                                <TextBlock x:Name="PlaceholderText"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="{DynamicResource MutedTextBrush}"
                                           Margin="{TemplateBinding Padding}"
                                           TextAlignment="{TemplateBinding TextAlignment}"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           IsHitTestVisible="False"
                                           Opacity="0.65"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InputBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="DropDownToggle"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Padding="0">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Border x:Name="ComboBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="7"
                                        MinHeight="{TemplateBinding MinHeight}">
                                    <Grid>
                                        <ContentPresenter x:Name="ContentSite"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                                          IsHitTestVisible="False"
                                                          Margin="10,0,34,0"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"/>
                                        <Path Width="10"
                                              Height="6"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Center"
                                              Margin="0,0,12,0"
                                              Fill="{DynamicResource MutedTextBrush}"
                                              Stretch="Fill"
                                              Data="M 0 0 L 10 0 L 5 6 Z"/>
                                    </Grid>
                                </Border>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      SnapsToDevicePixels="True">
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="7"
                                            Padding="4">
                                        <ScrollViewer CanContentScroll="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"
                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ComboBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ComboBoxItem}">
                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                    <Border x:Name="ItemBorder"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter Content="{TemplateBinding Content}"
                                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                                          ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                          ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarPageButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbBorder"
                                Background="{DynamicResource ScrollBarThumbBrush}"
                                CornerRadius="6"
                                Margin="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{DynamicResource ScrollBarTrackBrush}"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="ScrollRoot"
                              Background="{TemplateBinding Background}"
                              SnapsToDevicePixels="True">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="DecreaseButton"
                                                  Command="{x:Static ScrollBar.PageUpCommand}"
                                                  Style="{StaticResource ScrollBarPageButtonStyle}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="IncreaseButton"
                                                  Command="{x:Static ScrollBar.PageDownCommand}"
                                                  Style="{StaticResource ScrollBarPageButtonStyle}"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="ScrollRoot" Property="Height" Value="12"/>
                                <Setter TargetName="ScrollRoot" Property="Width" Value="Auto"/>
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                                <Setter TargetName="DecreaseButton" Property="Command" Value="{x:Static ScrollBar.PageLeftCommand}"/>
                                <Setter TargetName="IncreaseButton" Property="Command" Value="{x:Static ScrollBar.PageRightCommand}"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="ScrollRoot" Property="Width" Value="12"/>
                                <Setter TargetName="ScrollRoot" Property="Height" Value="Auto"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="PanningMode" Value="Both"/>
        </Style>

        <Style x:Key="DatePickerTextBoxStyle" TargetType="{x:Type primitives:DatePickerTextBox}">
            <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentDarkBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type primitives:DatePickerTextBox}">
                        <Border x:Name="DateInputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7,0,0,7">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"/>
                                <TextBlock x:Name="PART_Watermark"
                                           Text="Select date"
                                           Foreground="{DynamicResource MutedTextBrush}"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           IsHitTestVisible="False"
                                           Opacity="0.65"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DateInputBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="DateInputBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CalendarDayButtonStyle" TargetType="{x:Type primitives:CalendarDayButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type primitives:CalendarDayButton}">
                        <Border x:Name="DayBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="DayBorder" Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
                                <Setter Property="Opacity" Value="0.65"/>
                            </Trigger>
                            <Trigger Property="IsBlackedOut" Value="True">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CalendarButtonStyle" TargetType="{x:Type primitives:CalendarButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type primitives:CalendarButton}">
                        <Border x:Name="CalendarButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CalendarButtonBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
                                <Setter Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemedCalendarStyle" TargetType="{x:Type Calendar}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="CalendarDayButtonStyle" Value="{StaticResource CalendarDayButtonStyle}"/>
            <Setter Property="CalendarButtonStyle" Value="{StaticResource CalendarButtonStyle}"/>
        </Style>

        <Style TargetType="{x:Type DatePicker}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ControlBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="CalendarStyle" Value="{StaticResource ThemedCalendarStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePicker}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="34"/>
                            </Grid.ColumnDefinitions>
                            <primitives:DatePickerTextBox x:Name="PART_TextBox"
                                                          Grid.Column="0"
                                                          Style="{StaticResource DatePickerTextBoxStyle}"
                                                          Foreground="{TemplateBinding Foreground}"
                                                          Background="{TemplateBinding Background}"
                                                          BorderBrush="{TemplateBinding BorderBrush}"
                                                          BorderThickness="{TemplateBinding BorderThickness}"/>
                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Focusable="False"
                                    Background="{DynamicResource ControlBrush}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0,1,1,1"
                                    Padding="0"
                                    MinHeight="{TemplateBinding MinHeight}">
                                <Path Width="13"
                                      Height="13"
                                      Fill="{DynamicResource MutedTextBrush}"
                                      Stretch="Uniform"
                                      Data="M1,2 L3,2 L3,0 L4,0 L4,2 L10,2 L10,0 L11,0 L11,2 L13,2 L13,13 L1,13 Z M2,5 L12,5 L12,12 L2,12 Z"/>
                            </Button>
                            <Popup x:Name="PART_Popup"
                                   Grid.ColumnSpan="2"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="7"
                                        Padding="6">
                                    <Calendar x:Name="PART_Calendar"
                                              Style="{TemplateBinding CalendarStyle}"
                                              SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}}"
                                              DisplayDate="{Binding DisplayDate, RelativeSource={RelativeSource TemplatedParent}}"
                                              DisplayDateStart="{Binding DisplayDateStart, RelativeSource={RelativeSource TemplatedParent}}"
                                              DisplayDateEnd="{Binding DisplayDateEnd, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Button" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="PART_TextBox" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="PART_Button" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,6,16,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBoxBorder"
                                    Width="18"
                                    Height="18"
                                    Background="{DynamicResource ControlBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Grid>
                                    <Path x:Name="CheckMark"
                                          Data="M 4 9 L 7.5 12.5 L 14 5"
                                          Stroke="{DynamicResource ChipTextBrush}"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Visibility="Collapsed"/>
                                    <Rectangle x:Name="IndeterminateMark"
                                               Width="9"
                                               Height="2"
                                               Fill="{DynamicResource ChipTextBrush}"
                                               RadiusX="1"
                                               RadiusY="1"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              Margin="8,0,0,0"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource DangerSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}"/>
        </Style>

        <Style x:Key="GridActionButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="FontSize" Value="10.5"/>
        </Style>

        <Style x:Key="GridDangerActionButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource DangerButtonStyle}">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="FontSize" Value="10.5"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource LinkHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarSectionTitleStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MutedTextStyle}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
        </Style>

        <Style x:Key="SidebarActionButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="SidebarQuickActionButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource SidebarActionButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="SidebarPanelItemTemplate">
            <Border Background="{DynamicResource ControlBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="8"
                    Margin="0,0,0,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Background="{Binding Accent}"
                            Width="3"
                            CornerRadius="2"
                            Margin="0,1,8,1"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Label}"
                                   Foreground="{DynamicResource TextBrush}"
                                   FontWeight="SemiBold"
                                   FontSize="11.5"/>
                        <TextBlock Text="{Binding Detail}"
                                   Style="{StaticResource MutedTextStyle}"
                                   TextWrapping="Wrap"
                                   TextTrimming="None"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="TrackingChipTemplate">
            <Border Background="{DynamicResource TrackingChipBrush}"
                    BorderBrush="{DynamicResource TrackingChipBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="5,2"
                    Margin="0,0,4,4"
                    HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Carrier}"
                               Foreground="{DynamicResource MutedTextBrush}"
                               FontSize="10.5"
                               FontStretch="SemiCondensed"
                               MaxWidth="42"
                               TextTrimming="CharacterEllipsis"
                               Margin="0,0,4,0"/>
                    <Button Command="{Binding Data.OpenTrackingCommand, Source={StaticResource VmProxy}}"
                            Grid.Column="1"
                            CommandParameter="{Binding}"
                            Style="{StaticResource LinkButtonStyle}"
                            FontSize="10.5"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            ToolTip="{Binding Number}">
                        <TextBlock Text="{Binding Number}"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                                   FontSize="10.5"
                                   FontStretch="SemiCondensed"
                                   MaxWidth="170"
                                   TextTrimming="CharacterEllipsis"/>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>

        <Style x:Key="ExpandToggleButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="ToggleBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Path x:Name="ArrowPath"
                                  Width="7"
                                  Height="9"
                                  Fill="{DynamicResource MutedTextBrush}"
                                  Stretch="Fill"
                                  RenderTransformOrigin="0.5,0.5"
                                  Data="M 0 0 L 8 5 L 0 10 Z"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                <Setter TargetName="ArrowPath" Property="Fill" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource AccentDarkBrush}"/>
                                <Setter TargetName="ArrowPath" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ContextMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="MinWidth" Value="170"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="MenuItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ContextMenuItemStyle}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource ContextMenuItemStyle}"/>

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HasDropShadow" Value="False"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToolTip}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Content"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Expander}">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Expander}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <DockPanel>
                                <ToggleButton x:Name="HeaderToggle"
                                              DockPanel.Dock="Top"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                              ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                              Cursor="Hand"
                                              Focusable="False"
                                              IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Padding="{TemplateBinding Padding}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="HeaderBorder"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Path x:Name="HeaderArrow"
                                                          Width="7"
                                                          Height="9"
                                                          Fill="{DynamicResource MutedTextBrush}"
                                                          Stretch="Fill"
                                                          RenderTransformOrigin="0.5,0.5"
                                                          Data="M 0 0 L 8 5 L 0 10 Z"
                                                          Margin="0,0,8,0"
                                                          VerticalAlignment="Center"/>
                                                    <ContentPresenter Grid.Column="1"
                                                                      Content="{TemplateBinding Content}"
                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                      ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                                      ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                                      TextElement.Foreground="{DynamicResource TextBrush}"
                                                                      VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}"/>
                                                    <Setter TargetName="HeaderArrow" Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="HeaderArrow" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <RotateTransform Angle="90"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ExpandSite"
                                                  ContentSource="Content"
                                                  Visibility="Collapsed"/>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowBackground" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGridAlternateBrush}"/>
            <Setter Property="SelectionMode" Value="Extended"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
        </Style>

        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{DynamicResource DataGridHeaderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="8,7"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OrderRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="MinHeight" Value="54"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <EventSetter Event="MouseDoubleClick" Handler="OrderRowDoubleClick"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Select"
                                  Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Unselect"
                                  Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Edit"
                                  Command="{Binding Data.EditOrderCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Open order link"
                                  Command="{Binding Data.OpenOrderLinkCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Copy tracking numbers"
                                  Command="{Binding Data.CopyTrackingNumbersCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Toggle completed"
                                  Command="{Binding Data.ToggleCompletedCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Duplicate"
                                  Command="{Binding Data.DuplicateOrderCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Delete"
                                  Command="{Binding Data.DeleteOrderCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowSelectedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ArchiveOrderRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="MinHeight" Value="54"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Select"
                                  Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Unselect"
                                  Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Restore to Orders"
                                  Command="{Binding Data.RestoreOrderCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Open order link"
                                  Command="{Binding Data.OpenOrderLinkCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Copy tracking numbers"
                                  Command="{Binding Data.CopyTrackingNumbersCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Delete"
                                  Command="{Binding Data.DeleteOrderCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowSelectedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PresetRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Select"
                                  Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Unselect"
                                  Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Edit"
                                  Command="{Binding Data.EditPresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Apply to current order"
                                  Command="{Binding Data.ApplyPresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Duplicate"
                                  Command="{Binding Data.DuplicatePresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Delete"
                                  Command="{Binding Data.DeletePresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowSelectedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccountPresetRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Select"
                                  Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Unselect"
                                  Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}"/>
                        <MenuItem Header="Edit"
                                  Command="{Binding Data.EditAccountPresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Apply to current order"
                                  Command="{Binding Data.ApplyAccountPresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="View Amazon orders"
                                  Command="{Binding Data.ViewAccountOrdersCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Duplicate"
                                  Command="{Binding Data.DuplicateAccountPresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Delete"
                                  Command="{Binding Data.DeleteAccountPresetCommand, Source={StaticResource VmProxy}}"
                                  CommandParameter="{Binding}"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowSelectedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CellTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="FontStretch" Value="SemiCondensed"/>
        </Style>

        <Style x:Key="CompactCellTextStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
            <Setter Property="FontSize" Value="11.5"/>
        </Style>

        <Style x:Key="CellTextWithToolTipStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="ToolTip" Value="{x:Null}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CompactCellTextWithToolTipStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CompactCellTextStyle}">
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="ToolTip" Value="{x:Null}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="OrderGridItemTextStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontStretch" Value="Condensed"/>
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="0"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="ToolTip" Value="{x:Null}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="OrderGridSubTextStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MutedTextStyle}">
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="FontStretch" Value="SemiCondensed"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style x:Key="OrderGridLinkTextStyle" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource CellTextStyle}">
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
            <Setter Property="FontSize" Value="11.5"/>
        </Style>

        <Style x:Key="DashboardMetricValueStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="Margin" Value="0,6,0,2"/>
        </Style>

        <Style x:Key="ChartValueTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="ChartBarStyle" TargetType="{x:Type ProgressBar}">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ProgressTrackBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid x:Name="PART_Track"
                              ClipToBounds="True"
                              SnapsToDevicePixels="True">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="5"
                                    HorizontalAlignment="Left"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="236"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource SidebarBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,1,0"
                Padding="18">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="Assets/app-icon.png"
                               Width="42"
                               Height="42"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               Margin="0,0,10,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Order Tracker"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="Purchases, tracking, and spend"
                                       Style="{StaticResource MutedTextStyle}"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="Dashboard"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Dashboard"/>
                    <Button Content="Orders"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Orders"/>
                    <Button Content="Archive"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Archive"/>
                    <Button Content="Accounts"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Accounts"/>
                    <Button Content="Item presets"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Presets"/>
                    <Button Content="Settings"
                            Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="Settings"/>
                </StackPanel>

                <Border DockPanel.Dock="Bottom"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12"
                        Margin="0,16,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer Grid.Row="0"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <StackPanel>
                                <TextBlock Text="Status"
                                           Style="{StaticResource MutedTextStyle}"/>
                                <TextBlock Text="{Binding LastActionMessage}"
                                           TextWrapping="Wrap"
                                           TextTrimming="None"
                                           Margin="0,6,0,0"
                                           Foreground="{DynamicResource TextBrush}"/>

                                <TextBlock Text="Alerts"
                                           Style="{StaticResource SidebarSectionTitleStyle}"/>
                                <ItemsControl ItemsSource="{Binding SidebarAlerts}"
                                              ItemTemplate="{StaticResource SidebarPanelItemTemplate}"/>

                                <TextBlock Text="Quick actions"
                                           Style="{StaticResource SidebarSectionTitleStyle}"/>
                                <Button Content="+ New order"
                                        Command="{Binding ToggleQuickOrderCommand}"
                                        Tag="{Binding IsOrderEditorOpen}"
                                        Style="{StaticResource SidebarQuickActionButtonStyle}"/>
                                <Button Content="+ New account"
                                        Command="{Binding ToggleQuickAccountPresetCommand}"
                                        Tag="{Binding IsAccountPresetEditorOpen}"
                                        Style="{StaticResource SidebarQuickActionButtonStyle}"/>
                                <Button Content="+ New preset"
                                        Command="{Binding ToggleQuickPresetCommand}"
                                        Tag="{Binding IsPresetEditorOpen}"
                                        Style="{StaticResource SidebarQuickActionButtonStyle}"/>
                                <Button Content="Archive ready"
                                        Command="{Binding ArchiveCompletedOrdersCommand}"
                                        Style="{StaticResource SidebarActionButtonStyle}"
                                        ToolTip="{Binding ArchiveCompletedOrdersLabel}"/>
                                <Button Content="Discord stats"
                                        Command="{Binding SendDiscordStatsCommand}"
                                        Style="{StaticResource SidebarActionButtonStyle}"
                                        Margin="0"/>
                            </StackPanel>
                        </ScrollViewer>

                        <Border Grid.Row="1"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="0,1,0,0"
                                Padding="0,10,0,0"
                                Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="{Binding SidebarDate}"
                                           Style="{StaticResource MutedTextStyle}"
                                           TextTrimming="None"/>
                                <TextBlock Text="{Binding SidebarTime}"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Background="{DynamicResource AppBackgroundBrush}">
            <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Dashboard}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="28,24,28,14">
                    <TextBlock Text="Dashboard" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock Text="A quick read on order status, spend, and delivery flow." Style="{StaticResource MutedTextStyle}"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" Margin="28,0,28,24" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding MetricCards}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="16"
                                            MinHeight="118"
                                            Margin="0,0,12,14">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Border Width="34"
                                                    Height="4"
                                                    Background="{Binding Accent}"
                                                    CornerRadius="2"
                                                    HorizontalAlignment="Left"
                                                    Margin="0,0,0,12"/>
                                            <StackPanel Grid.Row="1">
                                                <TextBlock Text="{Binding Label}" Style="{StaticResource MutedTextStyle}"/>
                                                <TextBlock Text="{Binding Value}"
                                                           Style="{StaticResource DashboardMetricValueStyle}"/>
                                            </StackPanel>
                                            <TextBlock Grid.Row="2" Text="{Binding Detail}" Style="{StaticResource MutedTextStyle}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.25*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="18"
                                    Margin="0,0,14,14">
                                <StackPanel>
                                    <TextBlock Text="Monthly spend" Style="{StaticResource SectionTitleStyle}"/>
                                    <ItemsControl ItemsSource="{Binding MonthlySpend}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,11">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="72"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="84"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Label}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center"/>
                                                    <ProgressBar Grid.Column="1"
                                                                 Value="{Binding Percent}"
                                                                 Foreground="{Binding Accent}"
                                                                 Style="{StaticResource ChartBarStyle}"
                                                                 Margin="0,0,14,0"/>
                                                    <TextBlock Grid.Column="2" Text="{Binding DisplayValue}" Style="{StaticResource ChartValueTextStyle}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="18"
                                        Margin="0,0,0,14">
                                    <StackPanel>
                                        <TextBlock Text="Status breakdown" Style="{StaticResource SectionTitleStyle}"/>
                                        <ItemsControl ItemsSource="{Binding StatusBreakdown}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="126"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="44"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Label}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center"/>
                                                        <ProgressBar Grid.Column="1"
                                                                     Value="{Binding Percent}"
                                                                     Foreground="{Binding Accent}"
                                                                     Style="{StaticResource ChartBarStyle}"
                                                                     Margin="0,0,12,0"/>
                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayValue}" Style="{StaticResource ChartValueTextStyle}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="18"
                                        Margin="0,0,0,14">
                                    <StackPanel>
                                        <TextBlock Text="Merchant spend" Style="{StaticResource SectionTitleStyle}"/>
                                        <ItemsControl ItemsSource="{Binding MerchantSpend}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="108"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="80"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Label}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center"/>
                                                        <ProgressBar Grid.Column="1"
                                                                     Value="{Binding Percent}"
                                                                     Foreground="{Binding Accent}"
                                                                     Style="{StaticResource ChartBarStyle}"
                                                                     Margin="0,0,12,0"/>
                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayValue}" Style="{StaticResource ChartValueTextStyle}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="18">
                                    <StackPanel>
                                        <TextBlock Text="Yearly totals" Style="{StaticResource SectionTitleStyle}"/>
                                        <ItemsControl ItemsSource="{Binding YearlySpend}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="70"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="80"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Label}" Style="{StaticResource MutedTextStyle}" VerticalAlignment="Center"/>
                                                        <ProgressBar Grid.Column="1"
                                                                     Value="{Binding Percent}"
                                                                     Foreground="{Binding Accent}"
                                                                     Style="{StaticResource ChartBarStyle}"
                                                                     Margin="0,0,12,0"/>
                                                        <TextBlock Grid.Column="2" Text="{Binding DisplayValue}" Style="{StaticResource ChartValueTextStyle}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Orders}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Margin="28,24,28,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Orders" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="Track merchants, costs, dates, tracking numbers, and completion state." Style="{StaticResource MutedTextStyle}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <Button Content="{Binding ArchiveCompletedOrdersLabel}"
                                Command="{Binding ArchiveCompletedOrdersCommand}"
                                Margin="0,0,8,0"/>
                        <Button Content="New order" Command="{Binding NewOrderCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
                        <Button Content="Save order"
                                Command="{Binding SaveOrderCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Visibility="{Binding IsOrderEditorOpen, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="28,0,28,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="{Binding Data.IsOrderEditorOpen, Source={StaticResource VmProxy}, Converter={StaticResource BoolToGridLength}, ConverterParameter=360}"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Column="0"
                            Grid.Row="0"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="12"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="170"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,0"
                                     ToolTip="Search orders"/>
                            <ComboBox Grid.Column="1"
                                      ItemsSource="{Binding GroupOptions}"
                                      SelectedItem="{Binding SelectedGroup}"
                                      Margin="0,0,10,0"/>
                            <ComboBox Grid.Column="2"
                                      ItemsSource="{Binding SortOptions}"
                                      SelectedItem="{Binding SelectedSort}"
                                      Margin="0,0,10,0"/>
                            <CheckBox Grid.Column="3"
                                      Content="Hide completed"
                                      IsChecked="{Binding HideCompleted}"
                                      VerticalAlignment="Center"/>
                            <WrapPanel Grid.Row="1"
                                       Grid.ColumnSpan="4"
                                       Margin="0,10,0,0"
                                       VerticalAlignment="Center">
                                <TextBlock Text="{Binding ActiveOrderBulkSelectionSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                                <Button Content="Select"
                                        Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                        CommandTarget="{Binding ElementName=OrdersGrid}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Unselect"
                                        Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                        CommandTarget="{Binding ElementName=OrdersGrid}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Select all"
                                        Command="{Binding SelectVisibleOrdersCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Clear"
                                        Command="{Binding ClearSelectedOrdersCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Mark completed"
                                        Command="{Binding MarkSelectedOrdersCompletedCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Archive selected completed"
                                        Command="{Binding ArchiveSelectedCompletedOrdersCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Delete selected"
                                        Command="{Binding DeleteSelectedOrdersCommand}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="8,4"
                                        MinHeight="28"/>
                            </WrapPanel>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="OrdersGrid"
                              Grid.Column="0"
                              Grid.Row="1"
                              ItemsSource="{Binding OrdersView}"
                              SelectedItem="{Binding SelectedOrder}"
                              RowStyle="{StaticResource OrderRowStyle}"
                              SelectionChanged="SelectableGridSelectionChanged"
                              AlternationCount="2"
                              RowDetailsVisibilityMode="Visible"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ColumnWidth="*"
                              Margin="0">
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="True"
                                                              Background="{DynamicResource SurfaceAltBrush}"
                                                              Foreground="{DynamicResource TextBrush}"
                                                              BorderBrush="{DynamicResource BorderBrush}"
                                                              BorderThickness="0,0,0,1"
                                                              Padding="10,8">
                                                        <Expander.Header>
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                                                <TextBlock Text="{Binding ItemCount, StringFormat=' ({0})'}"
                                                                           Foreground="{DynamicResource MutedTextBrush}"
                                                                           Margin="6,0,0,0"/>
                                                            </StackPanel>
                                                        </Expander.Header>
                                                        <ItemsPresenter/>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.RowDetailsTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <Border Background="{DynamicResource SurfaceAltBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,1"
                                            Padding="12"
                                            Margin="42,0,8,10"
                                            Visibility="{Binding IsItemsExpanded, Converter={StaticResource BoolToVisibility}}">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="70"/>
                                                    <ColumnDefinition Width="90"/>
                                                    <ColumnDefinition Width="90"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Item" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold"/>
                                                <TextBlock Grid.Column="1" Text="Qty" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" TextAlignment="Right"/>
                                                <TextBlock Grid.Column="2" Text="Unit" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" TextAlignment="Right"/>
                                                <TextBlock Grid.Column="3" Text="Subtotal" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold" TextAlignment="Right"/>
                                            </Grid>
                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,3,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="70"/>
                                                                <ColumnDefinition Width="90"/>
                                                                <ColumnDefinition Width="90"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Text="{Binding Name}" Style="{StaticResource CellTextWithToolTipStyle}"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding Quantity}" Style="{StaticResource CellTextStyle}" TextAlignment="Right"/>
                                                            <TextBlock Grid.Column="2" Text="{Binding UnitPrice, StringFormat=C}" Style="{StaticResource CellTextStyle}" TextAlignment="Right"/>
                                                            <TextBlock Grid.Column="3" Text="{Binding Subtotal, StringFormat=C, Mode=OneWay}" Style="{StaticResource CellTextStyle}" TextAlignment="Right"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>

                                    <Border Background="{DynamicResource SurfaceAltBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,1,0,1"
                                            Padding="12"
                                            Margin="42,0,8,10"
                                            Visibility="{Binding IsTrackingExpanded, Converter={StaticResource BoolToVisibility}}">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="160"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Tracking" Style="{StaticResource MutedTextStyle}" FontWeight="SemiBold"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding TrackingCountSummary}"
                                                           Style="{StaticResource MutedTextStyle}"
                                                           FontWeight="SemiBold"
                                                           TextAlignment="Right"/>
                                            </Grid>
                                            <ItemsControl ItemsSource="{Binding TrackingNumbers}"
                                                          ItemTemplate="{StaticResource TrackingChipTemplate}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </DataGrid.RowDetailsTemplate>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header=""
                                                    Width="36"
                                                    MinWidth="34"
                                                    MaxWidth="38">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center"
                                                  Margin="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Merchant"
                                                    Width="0.7*"
                                                    MinWidth="64"
                                                    MaxWidth="92"
                                                    Visibility="{Binding Data.Settings.Columns.ShowMerchant, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Width="30"
                                                    Height="30"
                                                    CornerRadius="15"
                                                    Background="{Binding Merchant, Converter={StaticResource MerchantBrush}}"
                                                    Margin="0,0,6,0">
                                                <TextBlock Text="{Binding MerchantIcon}"
                                                           Foreground="{DynamicResource ChipTextBrush}"
                                                           FontWeight="Bold"
                                                           FontSize="12"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding Merchant}"
                                                       Style="{StaticResource CompactCellTextStyle}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Account"
                                                Width="1.05*"
                                                MinWidth="100"
                                                Binding="{Binding AccountEmail}"
                                                ElementStyle="{StaticResource CompactCellTextWithToolTipStyle}"
                                                Visibility="{Binding Data.Settings.Columns.ShowAccount, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}"/>

                            <DataGridTemplateColumn Header="Order"
                                                    Width="1.15*"
                                                    MinWidth="124"
                                                    Visibility="{Binding Data.Settings.Columns.ShowOrderNumber, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding Data.OpenOrderLinkCommand, Source={StaticResource VmProxy}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource LinkButtonStyle}"
                                                HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Stretch"
                                                ToolTip="{Binding OrderNumber}">
                                            <TextBlock Text="{Binding OrderNumber}"
                                                       Style="{StaticResource OrderGridLinkTextStyle}"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Items"
                                                    Width="0.55*"
                                                    MinWidth="70"
                                                    MaxWidth="90"
                                                    Visibility="{Binding Data.Settings.Columns.ShowItem, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid VerticalAlignment="Center"
                                              ToolTip="{Binding ItemsToolTip}"
                                              ToolTipService.InitialShowDelay="0"
                                              ToolTipService.BetweenShowDelay="0"
                                              ToolTipService.ShowDuration="60000">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <ToggleButton IsChecked="{Binding IsItemsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          Visibility="{Binding HasItems, Converter={StaticResource BoolToVisibility}}"
                                                          Style="{StaticResource ExpandToggleButtonStyle}"
                                                          ToolTip="{Binding ItemsToolTip}"
                                                          Margin="0,0,6,0"/>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding ItemCountSummary}"
                                                       Style="{StaticResource CompactCellTextStyle}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Qty"
                                                Width="0.38*"
                                                MinWidth="44"
                                                MaxWidth="54"
                                                Binding="{Binding TotalQuantity, Mode=OneWay}"
                                                ElementStyle="{StaticResource CompactCellTextStyle}"
                                                Visibility="{Binding Data.Settings.Columns.ShowQuantity, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}"/>

                            <DataGridTemplateColumn Header="Status"
                                                    Width="0.95*"
                                                    MinWidth="92"
                                                    MaxWidth="112"
                                                    Visibility="{Binding Data.Settings.Columns.ShowStatus, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding Data.OrderStatuses, Source={StaticResource VmProxy}}"
                                                  SelectedItem="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Background="{Binding Status, Converter={StaticResource StatusBrush}}"
                                                  Foreground="{DynamicResource ChipTextBrush}"
                                                  BorderBrush="Transparent"
                                                  MinWidth="72"
                                                  MinHeight="28"
                                                  FontSize="11"
                                                  Padding="5,2"
                                                  ToolTip="Change status"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Tracking"
                                                    Width="1.45*"
                                                    MinWidth="150"
                                                    Visibility="{Binding Data.Settings.Columns.ShowTracking, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <ToggleButton IsChecked="{Binding IsTrackingExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          Visibility="{Binding HasMultipleTrackingNumbers, Converter={StaticResource BoolToVisibility}}"
                                                          Style="{StaticResource ExpandToggleButtonStyle}"
                                                          ToolTip="Show tracking numbers"
                                                          Margin="0,0,6,0"/>
                                            <StackPanel Grid.Column="1">
                                                <ContentControl Content="{Binding PrimaryTracking}"
                                                                ContentTemplate="{StaticResource TrackingChipTemplate}"
                                                                Visibility="{Binding HasTrackingNumbers, Converter={StaticResource BoolToVisibility}}"/>
                                                <TextBlock Text="{Binding TrackingCountSummary}"
                                                           Style="{StaticResource OrderGridSubTextStyle}"
                                                           Visibility="{Binding HasMultipleTrackingNumbers, Converter={StaticResource BoolToVisibility}}"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Dates"
                                                    Width="1.0*"
                                                    MinWidth="108"
                                                    MaxWidth="124"
                                                    Visibility="{Binding Data.Settings.Columns.ShowDates, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding OrderDate, StringFormat='Ordered {0:M/d/yy}'}" Style="{StaticResource CompactCellTextStyle}"/>
                                            <TextBlock Text="{Binding ExpectedDate, StringFormat='Expected {0:M/d/yy}'}" Style="{StaticResource OrderGridSubTextStyle}"/>
                                            <TextBlock Text="{Binding DeliveredDate, StringFormat='Delivered {0:M/d/yy}'}" Style="{StaticResource OrderGridSubTextStyle}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Total"
                                                Width="0.65*"
                                                MinWidth="74"
                                                MaxWidth="88"
                                                Binding="{Binding TotalCost, StringFormat=C, Mode=OneWay}"
                                                ElementStyle="{StaticResource CompactCellTextWithToolTipStyle}"
                                                Visibility="{Binding Data.Settings.Columns.ShowTotal, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}"/>

                            <DataGridTemplateColumn Header="Actions"
                                                    Width="1.15*"
                                                    MinWidth="122"
                                                    MaxWidth="132"
                                                    Visibility="{Binding Data.Settings.Columns.ShowActions, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Edit"
                                                    Command="{Binding Data.EditOrderCommand, Source={StaticResource VmProxy}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource GridActionButtonStyle}"
                                                    Margin="0,0,4,0"/>
                                            <Button Content="Done"
                                                    Command="{Binding Data.ToggleCompletedCommand, Source={StaticResource VmProxy}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource GridActionButtonStyle}"
                                                    Margin="0,0,4,0"/>
                                            <Button Content="Copy"
                                                    Command="{Binding Data.CopyTrackingNumbersCommand, Source={StaticResource VmProxy}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource GridActionButtonStyle}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Column="1"
                            Grid.RowSpan="2"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="18"
                            Margin="14,0,0,0"
                            Visibility="{Binding IsOrderEditorOpen, Converter={StaticResource BoolToVisibility}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding OrderEditorTitle}"
                                               Style="{StaticResource SectionTitleStyle}"
                                               Margin="0"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="1"
                                            Content="Close"
                                            Command="{Binding CloseOrderEditorCommand}"
                                            Padding="8,4"
                                            MinHeight="28"/>
                                </Grid>
                                <TextBlock Text="Preset" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding ItemPresets}"
                                          DisplayMemberPath="Name"
                                          SelectedItem="{Binding SelectedOrderPreset}"/>

                                <TextBlock Text="Account preset" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding AccountPresets}"
                                          DisplayMemberPath="DisplayName"
                                          SelectedItem="{Binding SelectedOrderAccountPreset}"/>

                                <TextBlock Text="Account/email" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding FormAccountEmail, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Merchant" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Merchants}" SelectedItem="{Binding FormMerchant}"/>

                                <TextBlock Text="Order number" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding FormOrderNumber, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Order link" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding FormOrderLink, UpdateSourceTrigger=PropertyChanged}"/>

                                <Grid Margin="0,10,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Items"
                                               Style="{StaticResource FieldLabelStyle}"
                                               Margin="0"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="1"
                                            Content="Add item"
                                            Command="{Binding AddOrderItemCommand}"
                                            Padding="9,5"
                                            MinHeight="28"/>
                                </Grid>

                                <ItemsControl ItemsSource="{Binding FormItems}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource SurfaceAltBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="7"
                                                    Padding="10"
                                                    Margin="0,0,0,8">
                                                <StackPanel>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                                                 ToolTip="Item name"
                                                                 Margin="0,0,8,0"/>
                                                        <Button Grid.Column="1"
                                                                Content="Remove"
                                                                Command="{Binding Data.RemoveOrderItemCommand, Source={StaticResource VmProxy}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource DangerButtonStyle}"
                                                                Padding="8,4"
                                                                MinHeight="30"/>
                                                    </Grid>
                                                    <Grid Margin="0,8,0,0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="80"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                            <TextBlock Text="Qty" Style="{StaticResource FieldLabelStyle}" Margin="0,0,0,4"/>
                                                            <TextBox Text="{Binding QuantityInput, UpdateSourceTrigger=PropertyChanged}"
                                                                     Style="{StaticResource PlaceholderTextBoxStyle}"
                                                                     Tag="1"
                                                                     TextAlignment="Right"/>
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="1">
                                                            <TextBlock Text="Unit price" Style="{StaticResource FieldLabelStyle}" Margin="0,0,0,4"/>
                                                            <TextBox Text="{Binding UnitPriceInput, UpdateSourceTrigger=PropertyChanged}"
                                                                     Style="{StaticResource PlaceholderTextBoxStyle}"
                                                                     Tag="0.00"
                                                                     TextAlignment="Right"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="Shipping" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding FormShippingCostInput, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                                 Tag="0.00"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                        <TextBlock Text="Tax" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding FormTaxInput, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                                 Tag="0.00"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Other" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding FormOtherCostInput, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                                 Tag="0.00"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="Order date" Style="{StaticResource FieldLabelStyle}"/>
                                        <DatePicker SelectedDate="{Binding FormOrderDate}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Expected" Style="{StaticResource FieldLabelStyle}"/>
                                        <DatePicker SelectedDate="{Binding FormExpectedDate}"/>
                                    </StackPanel>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="Status" Style="{StaticResource FieldLabelStyle}"/>
                                        <ComboBox ItemsSource="{Binding OrderStatuses}" SelectedItem="{Binding FormStatus}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Delivered" Style="{StaticResource FieldLabelStyle}"/>
                                        <DatePicker SelectedDate="{Binding FormDeliveredDate}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Tracking status" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding FormTrackingStatus, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Tracking numbers" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding FormTrackingNumbersText, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="78"
                                         VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Text="Notes" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding FormNotes, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="70"/>

                                <UniformGrid Columns="3" Margin="0,16,0,0">
                                    <Button Content="Save"
                                            Command="{Binding SaveOrderCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Margin="0,0,6,0"/>
                                    <Button Content="New"
                                            Command="{Binding NewOrderCommand}"
                                            Margin="3,0,3,0"/>
                                    <Button Content="Close"
                                            Command="{Binding CloseOrderEditorCommand}"
                                            Margin="6,0,0,0"/>
                                </UniformGrid>
                                <UniformGrid Columns="2" Margin="0,8,0,0">
                                    <Button Content="Toggle completed"
                                            Command="{Binding ToggleCompletedCommand}"
                                            CommandParameter="{Binding SelectedOrder}"
                                            Margin="0,0,6,0"/>
                                    <Button Content="Duplicate"
                                            Command="{Binding DuplicateOrderCommand}"
                                            CommandParameter="{Binding SelectedOrder}"
                                            Margin="6,0,0,0"/>
                                </UniformGrid>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>

            <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Archive}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="28,24,28,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Archive" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="{Binding ArchivedOrderSummary}" Style="{StaticResource MutedTextStyle}"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="14"
                        Margin="28,0,28,24">
                    <DockPanel>
                        <TextBox DockPanel.Dock="Top"
                                 Text="{Binding ArchiveSearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,12"
                                 ToolTip="Search archived orders"/>
                        <WrapPanel DockPanel.Dock="Top"
                                   Margin="0,0,0,12"
                                   VerticalAlignment="Center">
                            <TextBlock Text="{Binding ArchivedOrderBulkSelectionSummary}"
                                       Style="{StaticResource MutedTextStyle}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <Button Content="Select"
                                    Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                    CommandTarget="{Binding ElementName=ArchiveGrid}"
                                    Padding="8,4"
                                    MinHeight="28"
                                    Margin="0,0,6,0"/>
                            <Button Content="Unselect"
                                    Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                    CommandTarget="{Binding ElementName=ArchiveGrid}"
                                    Padding="8,4"
                                    MinHeight="28"
                                    Margin="0,0,6,0"/>
                            <Button Content="Select all"
                                    Command="{Binding SelectVisibleArchivedOrdersCommand}"
                                    Padding="8,4"
                                    MinHeight="28"
                                    Margin="0,0,6,0"/>
                            <Button Content="Clear"
                                    Command="{Binding ClearSelectedArchivedOrdersCommand}"
                                    Padding="8,4"
                                    MinHeight="28"
                                    Margin="0,0,6,0"/>
                            <Button Content="Restore selected"
                                    Command="{Binding RestoreSelectedOrdersCommand}"
                                    Padding="8,4"
                                    MinHeight="28"
                                    Margin="0,0,6,0"/>
                            <Button Content="Delete selected"
                                    Command="{Binding DeleteSelectedArchivedOrdersCommand}"
                                    Style="{StaticResource DangerButtonStyle}"
                                    Padding="8,4"
                                    MinHeight="28"/>
                        </WrapPanel>
                        <DataGrid x:Name="ArchiveGrid"
                                  ItemsSource="{Binding ArchivedOrdersView}"
                                  SelectedItem="{Binding SelectedOrder}"
                                  RowStyle="{StaticResource ArchiveOrderRowStyle}"
                                  SelectionChanged="SelectableGridSelectionChanged"
                                  AlternationCount="2"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ColumnWidth="*">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header=""
                                                        Width="36"
                                                        MinWidth="34"
                                                        MaxWidth="38">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      HorizontalAlignment="Center"
                                                      Margin="0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Merchant"
                                                        Width="0.7*"
                                                        MinWidth="64"
                                                        MaxWidth="92"
                                                        Visibility="{Binding Data.Settings.Columns.ShowMerchant, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid VerticalAlignment="Center">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Width="30"
                                                        Height="30"
                                                        CornerRadius="15"
                                                        Background="{Binding Merchant, Converter={StaticResource MerchantBrush}}"
                                                        Margin="0,0,6,0">
                                                    <TextBlock Text="{Binding MerchantIcon}"
                                                               Foreground="{DynamicResource ChipTextBrush}"
                                                               FontWeight="Bold"
                                                               FontSize="12"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                </Border>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Merchant}"
                                                           Style="{StaticResource CompactCellTextStyle}"
                                                           VerticalAlignment="Center"/>
                                            </Grid>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Account"
                                                    Width="1.05*"
                                                    MinWidth="100"
                                                    Binding="{Binding AccountEmail}"
                                                    ElementStyle="{StaticResource CompactCellTextWithToolTipStyle}"
                                                    Visibility="{Binding Data.Settings.Columns.ShowAccount, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}"/>

                                <DataGridTemplateColumn Header="Order"
                                                        Width="1.15*"
                                                        MinWidth="124"
                                                        Visibility="{Binding Data.Settings.Columns.ShowOrderNumber, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Command="{Binding Data.OpenOrderLinkCommand, Source={StaticResource VmProxy}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource LinkButtonStyle}"
                                                    HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="Stretch"
                                                    ToolTip="{Binding OrderNumber}">
                                                <TextBlock Text="{Binding OrderNumber}"
                                                           Style="{StaticResource OrderGridLinkTextStyle}"/>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Items"
                                                        Width="0.55*"
                                                        MinWidth="70"
                                                        MaxWidth="90"
                                                        Visibility="{Binding Data.Settings.Columns.ShowItem, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ItemCountSummary}"
                                                       Style="{StaticResource CompactCellTextStyle}"
                                                       VerticalAlignment="Center"
                                                       ToolTip="{Binding ItemsToolTip}"
                                                       ToolTipService.InitialShowDelay="0"
                                                       ToolTipService.BetweenShowDelay="0"
                                                       ToolTipService.ShowDuration="60000"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Qty"
                                                    Width="0.38*"
                                                    MinWidth="44"
                                                    MaxWidth="54"
                                                    Binding="{Binding TotalQuantity, Mode=OneWay}"
                                                    ElementStyle="{StaticResource CompactCellTextStyle}"
                                                    Visibility="{Binding Data.Settings.Columns.ShowQuantity, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}"/>

                                <DataGridTemplateColumn Header="Status"
                                                        Width="0.9*"
                                                        MinWidth="88"
                                                        MaxWidth="112"
                                                        Visibility="{Binding Data.Settings.Columns.ShowStatus, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding Status, Converter={StaticResource StatusBrush}}"
                                                    CornerRadius="7"
                                                    Padding="6,4"
                                                    HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Status}"
                                                           Foreground="{DynamicResource ChipTextBrush}"
                                                           FontWeight="SemiBold"
                                                           FontSize="11.5"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Tracking"
                                                        Width="1.45*"
                                                        MinWidth="150"
                                                        Visibility="{Binding Data.Settings.Columns.ShowTracking, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <ContentControl Content="{Binding PrimaryTracking}"
                                                                ContentTemplate="{StaticResource TrackingChipTemplate}"
                                                                Visibility="{Binding HasTrackingNumbers, Converter={StaticResource BoolToVisibility}}"/>
                                                <TextBlock Text="{Binding TrackingCountSummary}"
                                                           Style="{StaticResource OrderGridSubTextStyle}"
                                                           Visibility="{Binding HasMultipleTrackingNumbers, Converter={StaticResource BoolToVisibility}}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Dates"
                                                        Width="0.95*"
                                                        MinWidth="102"
                                                        MaxWidth="120"
                                                        Visibility="{Binding Data.Settings.Columns.ShowDates, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock Text="{Binding OrderDate, StringFormat='Ordered {0:M/d/yy}'}" Style="{StaticResource CompactCellTextStyle}"/>
                                                <TextBlock Text="{Binding DeliveredDate, StringFormat='Delivered {0:M/d/yy}'}" Style="{StaticResource OrderGridSubTextStyle}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Total"
                                                    Width="0.65*"
                                                    MinWidth="74"
                                                    MaxWidth="88"
                                                    Binding="{Binding TotalCost, StringFormat=C, Mode=OneWay}"
                                                    ElementStyle="{StaticResource CompactCellTextWithToolTipStyle}"
                                                    Visibility="{Binding Data.Settings.Columns.ShowTotal, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}"/>

                                <DataGridTemplateColumn Header="Actions"
                                                        Width="1.45*"
                                                        MinWidth="166"
                                                        MaxWidth="184"
                                                        Visibility="{Binding Data.Settings.Columns.ShowActions, Source={StaticResource VmProxy}, Converter={StaticResource BoolToVisibility}}">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Restore"
                                                        Command="{Binding Data.RestoreOrderCommand, Source={StaticResource VmProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource GridActionButtonStyle}"
                                                        Margin="0,0,4,0"/>
                                                <Button Content="Copy"
                                                        Command="{Binding Data.CopyTrackingNumbersCommand, Source={StaticResource VmProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource GridActionButtonStyle}"
                                                        Margin="0,0,4,0"/>
                                                <Button Content="Delete"
                                                        Command="{Binding Data.DeleteOrderCommand, Source={StaticResource VmProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource GridDangerActionButtonStyle}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>

            <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Accounts}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="28,24,28,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Accounts" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="Reusable account emails with merchant hints, notes, favorites, and usage counts." Style="{StaticResource MutedTextStyle}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <Button Content="New account" Command="{Binding NewAccountPresetCommand}" Margin="0,0,8,0"/>
                        <Button Content="Save account"
                                Command="{Binding SaveAccountPresetCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Visibility="{Binding IsAccountPresetEditorOpen, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="28,0,28,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="{Binding Data.IsAccountPresetEditorOpen, Source={StaticResource VmProxy}, Converter={StaticResource BoolToGridLength}, ConverterParameter=380}"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="14"
                            Margin="0">
                        <DockPanel>
                            <TextBox DockPanel.Dock="Top"
                                     Text="{Binding AccountPresetSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,0,12"
                                     ToolTip="Search accounts"/>
                            <WrapPanel DockPanel.Dock="Top"
                                       Margin="0,0,0,12"
                                       VerticalAlignment="Center">
                                <TextBlock Text="{Binding AccountPresetBulkSelectionSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                                <Button Content="Select"
                                        Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                        CommandTarget="{Binding ElementName=AccountsGrid}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Unselect"
                                        Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                        CommandTarget="{Binding ElementName=AccountsGrid}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Select all"
                                        Command="{Binding SelectVisibleAccountPresetsCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Clear"
                                        Command="{Binding ClearSelectedAccountPresetsCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Delete selected"
                                        Command="{Binding DeleteSelectedAccountPresetsCommand}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="8,4"
                                        MinHeight="28"/>
                            </WrapPanel>
                            <DataGrid x:Name="AccountsGrid"
                                      ItemsSource="{Binding AccountPresetsView}"
                                      SelectedItem="{Binding SelectedAccountPreset}"
                                      AutoGenerateColumns="False"
                                      RowStyle="{StaticResource AccountPresetRowStyle}"
                                      SelectionChanged="SelectableGridSelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="" Width="40">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          HorizontalAlignment="Center"
                                                          Margin="0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Name" Binding="{Binding DisplayName, Mode=OneWay}" Width="*" ElementStyle="{StaticResource CellTextWithToolTipStyle}"/>
                                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220" ElementStyle="{StaticResource CellTextWithToolTipStyle}"/>
                                    <DataGridTextColumn Header="Merchant" Binding="{Binding MerchantHint}" Width="120" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTemplateColumn Header="Fav" Width="60">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsFavorite, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          HorizontalAlignment="Center"
                                                          Margin="0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Used" Binding="{Binding UsageCount}" Width="70" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTemplateColumn Header="Actions" Width="314">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="Edit"
                                                            Command="{Binding Data.EditAccountPresetCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Margin="0,0,5,0"/>
                                                    <Button Content="Apply"
                                                            Command="{Binding Data.ApplyAccountPresetCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Margin="0,0,5,0"/>
                                                    <Button Content="View orders"
                                                            Command="{Binding Data.ViewAccountOrdersCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Margin="0,0,5,0"/>
                                                    <Button Content="Delete"
                                                            Command="{Binding Data.DeleteAccountPresetCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Style="{StaticResource DangerButtonStyle}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </Border>

                    <Border Grid.Column="1"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="18"
                            Margin="14,0,0,0"
                            Visibility="{Binding IsAccountPresetEditorOpen, Converter={StaticResource BoolToVisibility}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding AccountPresetEditorTitle}"
                                               Style="{StaticResource SectionTitleStyle}"
                                               Margin="0"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="1"
                                            Content="Close"
                                            Command="{Binding CloseAccountPresetEditorCommand}"
                                            Padding="8,4"
                                            MinHeight="28"/>
                                </Grid>
                                <TextBlock Text="Name" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding AccountPresetName, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Email" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding AccountPresetEmail, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Merchant hint" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Merchants}" SelectedItem="{Binding AccountPresetMerchantHint}"/>

                                <CheckBox Content="Favorite" IsChecked="{Binding AccountPresetIsFavorite}" Margin="0,14,0,4"/>

                                <TextBlock Text="Notes" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding AccountPresetNotes, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="92"/>

                                <UniformGrid Columns="3" Margin="0,16,0,0">
                                    <Button Content="Save"
                                            Command="{Binding SaveAccountPresetCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Margin="0,0,6,0"/>
                                    <Button Content="New"
                                            Command="{Binding NewAccountPresetCommand}"
                                            Margin="3,0,3,0"/>
                                    <Button Content="Close"
                                            Command="{Binding CloseAccountPresetEditorCommand}"
                                            Margin="6,0,0,0"/>
                                </UniformGrid>
                                <UniformGrid Columns="2" Margin="0,8,0,0">
                                    <Button Content="Apply"
                                            Command="{Binding ApplyAccountPresetCommand}"
                                            CommandParameter="{Binding SelectedAccountPreset}"
                                            Margin="0,0,6,0"/>
                                    <Button Content="Duplicate"
                                            Command="{Binding DuplicateAccountPresetCommand}"
                                            CommandParameter="{Binding SelectedAccountPreset}"
                                            Margin="6,0,0,0"/>
                                </UniformGrid>
                                <Button Content="View Amazon orders"
                                        Command="{Binding ViewAccountOrdersCommand}"
                                        CommandParameter="{Binding SelectedAccountPreset}"
                                        Margin="0,8,0,0"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>

            <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Presets}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="28,24,28,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Item Presets" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="Reusable item templates with default quantities, costs, merchant hints, and notes." Style="{StaticResource MutedTextStyle}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <Button Content="New preset" Command="{Binding NewPresetCommand}" Margin="0,0,8,0"/>
                        <Button Content="Save preset"
                                Command="{Binding SavePresetCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Visibility="{Binding IsPresetEditorOpen, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="28,0,28,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="{Binding Data.IsPresetEditorOpen, Source={StaticResource VmProxy}, Converter={StaticResource BoolToGridLength}, ConverterParameter=380}"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="14"
                            Margin="0">
                        <DockPanel>
                            <TextBox DockPanel.Dock="Top"
                                     Text="{Binding PresetSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,0,12"
                                     ToolTip="Search presets"/>
                            <WrapPanel DockPanel.Dock="Top"
                                       Margin="0,0,0,12"
                                       VerticalAlignment="Center">
                                <TextBlock Text="{Binding PresetBulkSelectionSummary}"
                                           Style="{StaticResource MutedTextStyle}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                                <Button Content="Select"
                                        Command="{x:Static local:MainWindow.SelectHighlightedRowsCommand}"
                                        CommandTarget="{Binding ElementName=PresetsGrid}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Unselect"
                                        Command="{x:Static local:MainWindow.UnselectHighlightedRowsCommand}"
                                        CommandTarget="{Binding ElementName=PresetsGrid}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Select all"
                                        Command="{Binding SelectVisiblePresetsCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Clear"
                                        Command="{Binding ClearSelectedPresetsCommand}"
                                        Padding="8,4"
                                        MinHeight="28"
                                        Margin="0,0,6,0"/>
                                <Button Content="Delete selected"
                                        Command="{Binding DeleteSelectedPresetsCommand}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="8,4"
                                        MinHeight="28"/>
                            </WrapPanel>
                            <DataGrid x:Name="PresetsGrid"
                                      ItemsSource="{Binding PresetsView}"
                                      SelectedItem="{Binding SelectedPreset}"
                                      AutoGenerateColumns="False"
                                      RowStyle="{StaticResource PresetRowStyle}"
                                      SelectionChanged="SelectableGridSelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="" Width="40">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          HorizontalAlignment="Center"
                                                          Margin="0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTextColumn Header="Merchant" Binding="{Binding MerchantHint}" Width="120" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTextColumn Header="Qty" Binding="{Binding DefaultQuantity}" Width="60" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTextColumn Header="Price" Binding="{Binding DefaultUnitPrice, StringFormat=C}" Width="90" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTemplateColumn Header="Fav" Width="60">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsFavorite, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          HorizontalAlignment="Center"
                                                          Margin="0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Used" Binding="{Binding UsageCount}" Width="70" ElementStyle="{StaticResource CellTextStyle}"/>
                                    <DataGridTemplateColumn Header="Actions" Width="226">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="Edit"
                                                            Command="{Binding Data.EditPresetCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Margin="0,0,5,0"/>
                                                    <Button Content="Apply"
                                                            Command="{Binding Data.ApplyPresetCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Margin="0,0,5,0"/>
                                                    <Button Content="Delete"
                                                            Command="{Binding Data.DeletePresetCommand, Source={StaticResource VmProxy}}"
                                                            CommandParameter="{Binding}"
                                                            Padding="8,4"
                                                            MinHeight="26"
                                                            Style="{StaticResource DangerButtonStyle}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </Border>

                    <Border Grid.Column="1"
                            Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="18"
                            Margin="14,0,0,0"
                            Visibility="{Binding IsPresetEditorOpen, Converter={StaticResource BoolToVisibility}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding PresetEditorTitle}"
                                               Style="{StaticResource SectionTitleStyle}"
                                               Margin="0"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="1"
                                            Content="Close"
                                            Command="{Binding ClosePresetEditorCommand}"
                                            Padding="8,4"
                                            MinHeight="28"/>
                                </Grid>
                                <TextBlock Text="Name" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding PresetName, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Category" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding PresetCategory, UpdateSourceTrigger=PropertyChanged}"/>

                                <TextBlock Text="Merchant hint" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Merchants}" SelectedItem="{Binding PresetMerchantHint}"/>

                                <TextBlock Text="Default quantity" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding PresetDefaultQuantityInput, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource PlaceholderTextBoxStyle}"
                                         Tag="1"
                                         TextAlignment="Right"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="Price" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding PresetDefaultUnitPriceInput, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                                 Tag="0.00"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                        <TextBlock Text="Shipping" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding PresetDefaultShippingInput, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                                 Tag="0.00"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Tax" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding PresetDefaultTaxInput, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                                 Tag="0.00"
                                                 TextAlignment="Right"/>
                                    </StackPanel>
                                </Grid>

                                <CheckBox Content="Favorite" IsChecked="{Binding PresetIsFavorite}" Margin="0,14,0,4"/>

                                <TextBlock Text="Notes" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding PresetNotes, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="92"/>

                                <UniformGrid Columns="3" Margin="0,16,0,0">
                                    <Button Content="Save"
                                            Command="{Binding SavePresetCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Margin="0,0,6,0"/>
                                    <Button Content="New"
                                            Command="{Binding NewPresetCommand}"
                                            Margin="3,0,3,0"/>
                                    <Button Content="Close"
                                            Command="{Binding ClosePresetEditorCommand}"
                                            Margin="6,0,0,0"/>
                                </UniformGrid>
                                <Button Content="Apply to current order"
                                        Command="{Binding ApplyPresetCommand}"
                                        CommandParameter="{Binding SelectedPreset}"
                                        Margin="0,8,0,0"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>

            <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Settings}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="28,24,28,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Settings" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="Browser preference, Discord stats, defaults, storage, and visible order columns." Style="{StaticResource MutedTextStyle}"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Save settings"
                            Command="{Binding SaveSettingsCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            VerticalAlignment="Bottom"/>
                </Grid>

                <ScrollViewer Grid.Row="1"
                              Margin="28,0,28,24"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <Grid MaxWidth="1320">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="390"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,14,0">
                            <Border Style="{StaticResource SettingsCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Appearance and browser" Style="{StaticResource SectionTitleStyle}"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Margin="0,0,7,0">
                                            <TextBlock Text="Theme" Style="{StaticResource FieldLabelStyle}"/>
                                            <ComboBox ItemsSource="{Binding Themes}" SelectedItem="{Binding Settings.Theme}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="7,0,0,0">
                                            <TextBlock Text="Browser" Style="{StaticResource FieldLabelStyle}"/>
                                            <ComboBox ItemsSource="{Binding BrowserOptions}" SelectedItem="{Binding Settings.BrowserPreference}"/>
                                        </StackPanel>
                                    </Grid>
                                    <TextBlock Text="Custom browser path" Style="{StaticResource FieldLabelStyle}"/>
                                    <TextBox Text="{Binding Settings.CustomBrowserPath, UpdateSourceTrigger=PropertyChanged}"/>
                                    <CheckBox Content="Amazon account sessions"
                                              IsChecked="{Binding Settings.UseAccountBrowserSessions}"
                                              Margin="0,14,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource SettingsCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Discord webhook" Style="{StaticResource SectionTitleStyle}"/>
                                    <CheckBox Content="Enable Discord stats" IsChecked="{Binding Settings.DiscordEnabled}"/>
                                    <TextBlock Text="Webhook URL" Style="{StaticResource FieldLabelStyle}"/>
                                    <TextBox Text="{Binding Settings.DiscordWebhookUrl, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Button Content="Send stats now"
                                            Command="{Binding SendDiscordStatsCommand}"
                                            HorizontalAlignment="Left"
                                            MinWidth="140"
                                            Margin="0,14,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource SettingsCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Defaults and storage" Style="{StaticResource SectionTitleStyle}"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Margin="0,0,7,0">
                                            <TextBlock Text="Default account/email" Style="{StaticResource FieldLabelStyle}"/>
                                            <TextBox Text="{Binding Settings.DefaultAccountEmail, UpdateSourceTrigger=PropertyChanged}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="7,0,0,0">
                                            <TextBlock Text="Default merchant" Style="{StaticResource FieldLabelStyle}"/>
                                            <ComboBox ItemsSource="{Binding Merchants}" SelectedItem="{Binding Settings.DefaultMerchant}"/>
                                        </StackPanel>
                                    </Grid>
                                    <CheckBox Content="Auto-save changes" IsChecked="{Binding Settings.AutoSave}" Margin="0,14,0,0"/>
                                    <TextBlock Text="Data file" Style="{StaticResource FieldLabelStyle}"/>
                                    <TextBox Text="{Binding DataFilePath, Mode=OneWay}" IsReadOnly="True"/>
                                    <Grid Margin="0,10,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Config maintenance" Style="{StaticResource FieldLabelStyle}"/>
                                            <TextBlock Text="{Binding LegacyOrderItemMigrationStatus}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="1"
                                                Content="Update config"
                                                Command="{Binding MigrateLegacyOrderItemsCommand}"
                                                MinWidth="120"
                                                Margin="16,10,0,0"
                                                VerticalAlignment="Bottom"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Border Style="{StaticResource SettingsCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Orders list columns" Style="{StaticResource SectionTitleStyle}"/>
                                    <TextBlock Text="Toggle visible columns without changing your data."
                                               Style="{StaticResource MutedTextStyle}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,10"/>
                                    <UniformGrid Columns="2">
                                        <CheckBox Content="Merchant" IsChecked="{Binding Settings.Columns.ShowMerchant}"/>
                                        <CheckBox Content="Account" IsChecked="{Binding Settings.Columns.ShowAccount}"/>
                                        <CheckBox Content="Order number" IsChecked="{Binding Settings.Columns.ShowOrderNumber}"/>
                                        <CheckBox Content="Items" IsChecked="{Binding Settings.Columns.ShowItem}"/>
                                        <CheckBox Content="Quantity" IsChecked="{Binding Settings.Columns.ShowQuantity}"/>
                                        <CheckBox Content="Status" IsChecked="{Binding Settings.Columns.ShowStatus}"/>
                                        <CheckBox Content="Tracking" IsChecked="{Binding Settings.Columns.ShowTracking}"/>
                                        <CheckBox Content="Dates" IsChecked="{Binding Settings.Columns.ShowDates}"/>
                                        <CheckBox Content="Total" IsChecked="{Binding Settings.Columns.ShowTotal}"/>
                                        <CheckBox Content="Actions" IsChecked="{Binding Settings.Columns.ShowActions}"/>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource SettingsCardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Projected ROI" Style="{StaticResource SectionTitleStyle}"/>
                                    <ItemsControl ItemsSource="{Binding Settings.MerchantProjectedRoiPercents}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,8,0,0">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="72"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Merchant}"
                                                               Style="{StaticResource MutedTextStyle}"
                                                               VerticalAlignment="Center"/>
                                                    <TextBox Grid.Column="1"
                                                             Text="{Binding ProjectedRoiPercent, UpdateSourceTrigger=LostFocus, StringFormat={}{0:0.##}}"
                                                             HorizontalContentAlignment="Right"
                                                             Margin="10,0,6,0"/>
                                                    <TextBlock Grid.Column="2"
                                                               Text="%"
                                                               Style="{StaticResource MutedTextStyle}"
                                                               VerticalAlignment="Center"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>

        <Grid Panel.ZIndex="100"
              Grid.ColumnSpan="2"
              Background="{DynamicResource ModalOverlayBrush}"
              Visibility="{Binding IsConfirmationOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="22"
                    Width="420"
                    MaxWidth="520"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{Binding ConfirmationTitle}"
                               Style="{StaticResource SectionTitleStyle}"
                               FontSize="18"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding ConfirmationMessage}"
                               Foreground="{DynamicResource TextBrush}"
                               TextWrapping="Wrap"
                               LineHeight="20"
                               Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{Binding ConfirmationCancelText}"
                                Command="{Binding CancelDialogCommand}"
                                Margin="0,0,8,0"/>
                        <Button Content="{Binding ConfirmationConfirmText}"
                                Command="{Binding ConfirmDialogCommand}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Visibility="{Binding ConfirmationIsDanger, Converter={StaticResource BoolToVisibility}, ConverterParameter=Invert}"/>
                        <Button Content="{Binding ConfirmationConfirmText}"
                                Command="{Binding ConfirmDialogCommand}"
                                Style="{StaticResource DangerButtonStyle}"
                                Visibility="{Binding ConfirmationIsDanger, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
